上下文自动匹配已核实的官方规格，未知型号默认 256K，操作提示更简短。

- 每个模型按「手动设置 → 已核实的官方容量 → 未知型号默认 256K（256000 tokens）」确定上下文，自动压缩阈值为容量的 90%。
- 模型编辑窗口显示自动或手动容量及压缩阈值，新增“恢复自动”。已有手填值保留，清空后才改用自动规则。
- 内置官方规格按具体型号维护，并记录来源与核对日期；不在发送消息时联网查询。未收录的型号和自定义别名使用 256K 默认值，也可按上游限制手动调整。
- 保存、复制、删除、模型修改和模式切换等提示文案缩短。成功、普通、警告和错误提示统一显示 3 秒，鼠标悬停不延长；确认对话框仍等待用户操作。

升级：在 App“检查更新”中升级，或下载安装包覆盖安装。已有手填上下文不会被覆盖；需要自动设置时，在模型编辑窗口点击“恢复自动”并保存。重新开启挟持模式后，完全退出并重启 Codex 加载新容量。插件保持 0.3.8，现有连接与 Key 保留。

验证：Go 完整测试、静态检查、前端回归、三秒提示计时与焦点恢复检查、隔离 Codex 及桌面启动检查通过。模型验证只使用本地模拟接口。
