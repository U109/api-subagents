修复手动添加的模型缺少移除入口的问题。

- 模型列表增加移除按钮和确认弹窗，手动添加或修正 ID 的模型可彻底移除，同时清理显示名称。
- 已取消勾选的手动候选也能移除；上游目录中的模型移除后保留为未选候选，可随时重新勾选。
- 移除默认模型时自动选用剩余已选模型；删到最后一项时提示选择或添加新的默认模型后再保存。
- 取消或按 Esc 保留原配置，键盘焦点回到原行；移除后保持列表位置，并适配较窄窗口。

在右上角「检查更新」中升级 App，或下载安装包覆盖安装，已有连接与 Key 会保留。插件同步更新至 0.3.7，可在「插件管理」中独立更新。
