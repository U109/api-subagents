发布 App 0.4.9，附带插件 0.4.2，改善接入模型的排查效率与阶段反馈。

- App 0.4.9 与插件 0.4.2 增强接入模型的编码工作约定：重要发现和阻碍及时反馈，进度说明后继续工作，减少长时间只有工具记录、最后才统一回复的情况。
- 引导模型合并相关只读检查、减少重叠读取，遇到输出截断时缩小范围；证据充分后及时收敛，不以提速为由跳过权限和必要验证。实际效果仍取决于所选模型。
- 验证四类接口在先反馈、再调用工具、回传工具结果并继续回答时保留正文和工具数据；不把内部思考改成正文，不改写现有流式转发行为。

升级：可在 App 的「应用更新」中安装 0.4.9，或使用本页安装包覆盖安装。更新后重新开启挟持模式以刷新模型目录，并完全退出、重启 Codex 加载；不保证已运行任务立即采用新指令。执行型插件用户可在「插件管理」中独立更新至 0.4.2，并在 Codex 新建对话加载新进程。

本次不自动降低思考档位、不删除对话历史、不增加自动重试，也不承诺固定提速比例。本机连接、Key、Codex 配置备份、任务记录和构建工具不进入源码提交或安装包。
