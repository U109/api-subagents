增大界面字号，放开连接命名，并简化修改接口时的 Key 保留操作。

- 界面文字整体增大一档（2px），包括侧栏、表单、模型列表、按钮、弹窗和提示；同步调整按钮与下拉框高度。
- 连接名称支持中文、大小写、空格、符号和长名称，取消原来的格式和长度限制；仅保留空名称与重名检查。
- 复制连接保留完整名称；含斜线、百分号的连接名在 Codex 中也能正确选择和切换模型，旧连接继续兼容。
- 修改 API 地址或接口类型后，继续使用已保存的 Key 和原有环境变量设置，无需重新填写或清除；输入新 Key 时才替换原值。

在右上角「检查更新」中升级 App，或下载安装包覆盖安装，已有连接与 Key 会保留。本版插件同步更新至 0.3.5；使用插件委派时，请在「插件管理」中更新插件，并在 Codex 新建对话，以支持新的连接名称。
