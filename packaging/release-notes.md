改进挟持模式的切换提醒与接入状态，帮助定位 Upstream request failed。

- 每次开启、关闭挟持模式后，持续显示重启 Codex 和重新选择模型的提醒，修正之前“仅首次开启需要重启”的说明。
- 只有本地网关实际收到有效请求后，才显示“Codex 请求已接入本地网关”，便于识别只换了模型、没有切换提供商的情况。此状态不代表上游已经生成成功。
- 补充旧对话仍使用原提供商时的排查与处理说明：本地 api-subagents/… 模型标识不能直接发送给远程服务；切换模型不保证切换旧对话的提供商，必要时在重新开启并重启 Codex 后新建对话。
- 保留 Responses 透传行为和凭据隔离，上游兼容仍由上游负责。

升级：在 App“检查更新”中升级，或下载安装包覆盖安装。插件保持 0.3.8，现有连接与 Key 保留。

验证：Go 测试、静态检查、前端回归及隔离 Codex 的提供商切换与模型路由检查通过；模型验证只使用本地模拟接口。
