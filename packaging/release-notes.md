Responses 透传：让 CPA 等上游负责模型兼容，本地负责连接与转发。

- 新增独立的 Responses 透传链路，只替换路由模型 ID 与连接凭据，保留其他请求参数及上游 JSON、流式事件和错误响应。
- 透传不再补写思考参数、处理思考标签、缓存思考历史或合成完成事件；保留取消与超时，断流不自动重试。
- 新连接默认「Responses 透传（推荐 CPA）」，切换 Responses 与 Chat Completions 时保留地址和 Key。旧连接继续使用原接口，请手动切换到 Responses 才会启用透传。
- 一个连接的多个模型仍可在 Codex 中直接切换，保留每个模型的上下文设置。

使用要求：上游必须支持 /responses。如果上游在正文中返回 <thinking> 标签，透传仍会原样显示，需由上游修正。Chat Completions、Claude 和 Gemini 接口继续保留本地转换。

升级：在 App「检查更新」中升级，或下载安装包覆盖安装。插件保持 0.3.8；现有连接与 Key 保留。

验证：完整 Go 测试、静态检查、前端回归及隔离 Codex 集成测试通过，模型协议验证使用本地模拟接口。
