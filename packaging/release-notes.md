修复更新窗口沿用旧版本信息的问题，可以直接获取最新稳定版，无需逐个安装中间版本。

- 每次打开「检查更新」都会重新查询最新发布；发现更新或已有下载时，也能点击「重新检查」。
- 发现更高版本后改为提供最新安装包；同版本且校验信息一致的完整下载可继续使用。
- 检查失败时保留已下载包，安装前仍核对大小与 SHA-256；不会因为重新检查而自动安装。
- 插件版本保持 0.3.9，本机连接与 Key 保留。

升级：在 App 中检查更新。如果旧版仍显示此前发现的安装包，可完全退出并重新打开 App 后检查，或直接下载本页 v0.4.5 安装包覆盖安装，无需依次安装 0.4.2、0.4.3、0.4.4。

验证：Go 全量测试、静态检查、前端回归及桌面启动检查通过；本地模拟覆盖跨版本升级、旧包复用、网络失败、回退清单和篡改缓存。没有调用付费模型。
