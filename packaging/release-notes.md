一个 worker 配置多个模型，在 Codex 中直接切换；补齐模型兼容与上下文设置。

- 一个连接共用地址和 Key，勾选多个模型后可在 Codex 同一对话中直接切换。具体模型使用固定标识，修改 App 默认值不会误切已经选定的模型。
- 增加 Gemini、DeepSeek、Kimi、Kimi Coding、豆包、MiniMax、GLM 的兼容策略，支持思考参数转换、结构化思考和工具调用历史回传。官方地址自动识别，自定义网关保留 CPA 通用模式。
- 已选模型可单独设置上下文容量，自动压缩阈值随容量调整，避免所有模型都被固定为 32K。保存后重启 Codex 生效，关闭挟持模式恢复原配置。
- 移植 CPA 的工具名称、Gemini schema、Claude 与 Kimi 思考参数处理；补齐长工具名、思考字段和签名的保留，严格识别流式中断，不自动重复生成请求。
- 模型编辑窗口支持兼容策略和上下文设置，小窗口保留可见的保存按钮。

升级：在 App 右上角「检查更新」中升级，或下载安装包覆盖安装。插件同步更新至 0.3.8，可在「插件管理」独立更新；现有连接与 Key 保留。开启挟持模式后重启 Codex 刷新模型列表，之后在列表内切换模型无需重启。

验证：完整 Go 测试、静态检查、前端回归，以及隔离 Codex 的多模型切换测试通过。模型协议验证使用本地模拟接口，未调用真实付费 API；实际支持范围以所选上游为准。
