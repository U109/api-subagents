思考等级选择与历史对话修复。

- 精简侧栏和页脚，移除三处展开说明。
- 「模型选择」新增思考等级；Codex 模型列表也可选择，手动选择优先于连接默认。
- 四种协议传递相应推理参数；旧配置继续使用服务默认，不强制增加推理用量。
- 修复关闭挟持模式后旧对话报 `Model provider api_subagents not found`：恢复默认模型时保留无密钥的停用提供商，旧对话可以打开。
- 打开新版 App 会修复旧版遗留的对话入口，无需修改聊天记录。继续使用外部模型时需重新开启模式。

升级后请打开 App，再重启 Codex 刷新模型列表。思考档位需所选模型支持；使用插件委派时请同时点击「更新插件」。
