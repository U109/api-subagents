精简配置页面，统一弹窗与提示文字的结尾样式。

- 去掉「连接信息」「任务分工」「高级设置」面板内重复的标题、介绍和分隔线，打开 Tab 后直接显示配置字段。
- 删除、退出、模型编辑、插件管理和更新弹窗不再使用末尾中文句号。
- 浮动提示与后台返回的错误说明统一处理末尾句号，保留句子中间的标点、版本号和地址。
- 桌面 App 与插件的浏览器配置页同步采用新的布局和提示样式。

在右上角「检查更新」中升级 App，或下载安装包覆盖安装，已有连接与 Key 会保留。插件同步更新至 0.3.6，可在「插件管理」中独立更新。
