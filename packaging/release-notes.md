修复 Responses 请求未指定输出额度时，长回答或工具参数容易被上游默认额度截断的问题。

- 模型编辑窗口新增「输出额度」：自动（厂商资料）、火山 Coding Plan、保持上游默认、自定义。
- 按具体型号和平台选择参考预算；未知型号不猜测。请求已有额度时保留原值，压缩请求不添加额度。
- 正文、工具数据和返回流保持原样；不重试、不伪造完成状态。实际平台限制或输出超出预算仍可能返回截断错误。
- 插件更新至 0.3.9，支持新配置字段的读写；现有连接、Key 和上下文设置保留。

升级后，在模型编辑窗口按实际平台选择输出额度并保存。使用火山 Coding Plan 经 CPA 转发时，可手动选择「火山 Coding Plan」；无需改聊天记录。插件用户也请更新插件并在 Codex 中重新加载。

验证：本地模拟覆盖缺省与显式额度、压缩请求、原始数值与返回流、长工具参数；隔离 Codex、前端配置回归、Go 完整测试和桌面启动检查。未调用真实付费模型。
