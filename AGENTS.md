# 代码说明

- 手写 Go 与前端代码中，每个命名函数、类方法和对象方法上方都应有中文注释，说明职责以及必要的参数、返回结果或关键边界。
- 注释应帮助理解行为，不要只把方法名称翻译一遍。修改行为时同步更新注释。
- 业务代码按 `internal/` 中的职责包组织；前端通过固定 Go 绑定或带令牌的本地配置 API 操作。
- 流式响应、取消、文件访问、配置恢复和更新校验属于关键边界，改动后执行相应 `go test`。完整检查使用 `go test ./...` 与 `go vet ./...`。
- 发布只使用构建清单中的文件，禁止将本机连接、Key、Codex 配置备份、任务记录和构建工具放入 Git 或安装包。
- 模型测试使用本地模拟接口，不调用真实付费 API。测试 Codex 接入必须指定独立 `CODEX_HOME`。
